name: Cron Test (Every Minute)

on:
  schedule:
    - cron: "* * * * *"   # every minute
  workflow_dispatch:     # optional: allow manual run

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Just running
        run: echo "Cron job ran successfully"
